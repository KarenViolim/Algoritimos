print("Programa principal!")

# usuario = login(conexao, nome, senha)

            # Login
            # Senha
            # Digite seu login
            # Digite sua Senha
            # Buscar no banco de dados do usuário (select) se
            # existe um usuário no login WHERE login LIKE '%{}%';
            # WHERE senha LIKE '{}';
            # usuarios = cursor.fetchall() #tamanho for igual a 0 quer dizer que foi digitado invalido
            #tamanho do vetor maior que 0 quer dizer que ele achou o usuario
            # verificar se o tamanho do vetor for igual a 0 nada acontece
            # se o tamanho do vetor for maior que 0 mostrar o menu
            # retornar o vetor que você guardou o fetchall
            # u = login
            # u = 0 : não tem usuário
            # u = 1 : Existe o usuário ai.

            #BEM VINDO AO SISTEMA
            #OLÁ U NA POSIÇÃO 1 BEM VINDO!
